HANDOFF CONTEXT — Session Log 2026-08-26
=========================================

Project: MobiSentra AI
Session date: 2026-08-26 (session not yet in session list at time of writing)

USER REQUESTS (AS-IS)
---------------------
- "yea so what we did last time"
- "ok then complete that"
- "how long it will take ?" (asked mid-blocking; answered with options)
- "continue" (twice, after background-task retry notifications / todo continuation)
- "save the chat"

GOAL
----
Pick up Phase 2 (detection + tracking) from where the two dead 2026-08-25
sessions stopped, finish it: visual ID review, phase-2-completion.md, doc
syncs, handoff for owner commit.

WORK COMPLETED
--------------
- Reconstructed state: prior sessions' temp evidence was wiped; reproduced the
  gate numbers from scratch. Final tuned config (yolo26n, conf 0.3, imgsz 640,
  configs/botsort-tuned.yaml buffer 90): flicker-filtered stable_ratio 0.741,
  45 tracks, 12 stable (>=10 s), 7 reassociations — reproduced 3x identically.
- FPS gate re-measured: 57.25 FPS @720p, 44.09 FPS @1080p, 60 s sustained
  windows, device=mps. PASS (gate >= 15).
- Clone test re-run on HEAD (38 tests green) + full-tree post-commit
  simulation via rsync (54 tests + vision smoke green, ruff clean).
- E2E --detect re-verified: 3 cameras, 60 s, lag p95 <= 49 ms with detection
  attached, 3211 debug-JSONL records.
- Completed the tuning sweep the plan scheduled (all first-hand):
  * tracker family: stock bytetrack 0.679/103/45; stock botsort 0.603/68/19;
    botsort-tuned 0.741/45/7 (shipped winner); +reid 0.720/49/11 (rejected —
    also triggers yolo26n-cls.pt side-download)
  * imgsz (tuned): 640 -> 0.741/45/7; 960 -> 0.748/93/35; 1280 -> 0.635/82/11
  * conf (tuned): 0.25 -> 0.763/60/19; 0.30 -> 0.741/45/7; 0.35 -> 0.658/42/5
    (0.25 maximizes ratio but 2.7x reassociations; 0.3 ships)
  * buffer (conf 0.25): 90 -> 0.763; 120 -> 0.744 (no gain)
  * model: yolo11n 0.537 / yolo26n 0.679 / yolo26s 0.480 (stock basis);
    yolo11n on tuned 0.679 vs yolo26n 0.741 — YOLO26n decision validated
- Built edge/tools/track_review_dump.py: one-pass evidence-pack generator
  (annotated frames + events.json + summary.json). Verified: reproduces the
  0.741 pack exactly. Fixed one porting bug (zip strict=True on an offset
  walk) — index-walk rewrite.
- Wrote Doc/Implementation/phase-2-completion.md: gate evidence, full A/B
  tables, honest 0.80-miss analysis, 15-event owner review checklist (§7),
  gate-decision options A/B/C (§8).
- Synced 6 docs: YOLO26n decision row + MVP line (implementation-plan.md),
  runbook (header, tracker row, Step 2.1/2.2, Gate 2 checkboxes, progress
  tracker), phase-2-plan status header + approval boxes, root README
  (pipeline diagram + status table), Doc/Implementation/README table.
- Saved this session log.

VISUAL REVIEW — BLOCKED, CONVERTED TO OWNER CHECKLIST
-----------------------------------------------------
Automated review failed three independent ways:
1. look_at tool: timeouts on the frame batches (killed 2 prior sessions).
2. multimodal-looker agent: model routing broken — glm-4.6v and
   opencode/gpt-5.5 do not exist; retry chain bounced between invalid names
   and landed on gpt-5-nano; cancelled rather than accept a weak reviewer.
3. Main session model: no image input (verified directly).
Owner checklist (§7 of completion doc) + 90 annotated frames preserved at
edge/runs/phase2-review/ (gitignored; regenerate via
`uv run python tools/track_review_dump.py --out runs/phase2-review`).
~5 minutes of owner time. This is also the most legitimate form of the
gate's "manual overlay review".

CURRENT STATE
-------------
- Phase 2: EXECUTED. Gate 2 = PARTIAL: FPS + clone PASS; ID metric 0.741 vs
  >= 0.80 — honest miss on an adversarial flowing-traffic clip; best swept
  variant 0.763. Nothing single-knob reaches 0.80 (§5/§6 of completion doc).
- Working tree: 8 modified + 15 new files, ALL UNCOMMITTED (owner does 100%
  of git per permanent rule). Suggested commit message:
  `feat(phase-2): YOLO26n + tuned BoT-SORT detection & tracking with gate evidence`
  (optional doc split: `doc(phase-2): completion report, gate evidence, YOLO26n decision sync`)
- edge/runs/ and *.pt correctly gitignored (yolo26n.pt, yolo26n-cls.pt,
  yolo11n.pt, yolo26s.pt live in edge/ from A/B runs — harmless, untracked).
- Verification at close: ruff clean, 54 passed + 2 opt-in skips; vision smoke
  green under MOBISENTRA_VISION_TEST=1.

PENDING TASKS
-------------
- OWNER: §7 checklist (eyeball 15 events in edge/runs/phase2-review/review/).
- OWNER: §8 gate decision — A: waive 0.741 with rationale (recommended if
  checklist clean; Phase 3 zones are robust to fragmentation) / B: re-gate on
  a second queuing-crowd clip / C: keep tuning (out of scope, low yield).
- OWNER: commit (message above).
- Then Phase 3 (Zones/Occupancy/Door) per runbook — blocked until decision.
- Optional: fix multimodal-looker agent's model routing in opencode config
  (needs a valid vision model name) — affects future visual-QA work.

KEY FILES
---------
- Doc/Implementation/phase-2-completion.md — Phase 2 report; §7 checklist,
  §8 decision. Start here.
- Doc/Implementation/implementation-sequence.md — runbook; Phase 2 steps +
  Gate 2 status + progress tracker updated.
- edge/mobisentra/vision/{tracker,track_history}.py — core Phase 2 modules.
- edge/configs/{detection,botsort-tuned}.yaml — shipped config (A/B winner).
- edge/tools/{bench,track_stats,track_review_dump}.py — gate evidence tools.
- edge/runs/phase2-review/ — evidence pack (gitignored, regenerable).

IMPORTANT DECISIONS
-------------------
- Ship conf 0.3 over conf 0.25: ratio-optimal is 0.25 (0.763) but it 2.7x's
  reassociations (19 vs 7); fragmentation count is closer to the gate's
  "no ID swap between persistent people" intent. Documented, reversible.
- Reject with_reid: worse metric + classifier side-download cost.
- YOLO26n locked (decision table updated): +2.4 mAP, -31% CPU latency,
  NMS-free, A/B-validated; yolo11n stays a measured one-line fallback.
- imgsz 640 ships; 960/1280 fragment IDs on the 480p source.
- Honest gate: 0.741 < 0.80 recorded as MISS; no gate-shopping; owner
  decides waive/regate/tune. Phase 3 blocked meanwhile (gates are hard).

SESSION LESSONS (for next agent)
--------------------------------
- OS temp dirs get wiped between sessions — evidence belongs in repo tools
  (track_review_dump.py) + gitignored runs/, never only in /tmp.
- Re-verify any number inherited from a dead session's transcript; bash
  outputs do not survive — this session re-measured everything it cites.
- The multimodal agent stack is currently unusable for image review
  (routing broken); owner-eyeball checklists are the working substitute.
