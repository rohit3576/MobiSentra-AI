HANDOFF CONTEXT — Session Log 2026-09-01 (Phase 9 step 9.1b EXECUTED + verified; owner
scope decision PENDING — read PENDING TASKS before doing anything)
====================================================================

Project: MobiSentra AI
Session: continuation of the 2026-08-31 arc (Doc/handoff-2026-08-31.md).
This session: recapped state, executed 9.1b (live incident feed) fully
including the live <1 s gate, then the owner raised a SCOPE question
(no hardware available) and I proposed a trimmed finish — decision NOT
yet made. Owner has NOT committed 8.4b, 9.1a, or 9.1b (git ban: owner
does 100% of git).

USER REQUESTS (AS-IS)
---------------------
- "where was we last time"
- "ok"
- "ahh we cant do that now , we dont have any devices or anything we
  just have this so we only can upload video and check it live or we
  can upload and get the output , so if we are in that stage maybe we
  are done now"
- "save chat"

GOAL
----
Get the owner's decision among the three proposed finish options, then
execute whichever they pick (recommendation was Option 1, the minimal
finish: E2E video arc -> 9.3-lite ack/escalate -> 9.4-lite evidence
replay -> 9.5 trimmed -> defer 9.2 -> demo replay + v0.1.0).

WORK COMPLETED
--------------
- I recapped where the 08-31 session left off (9.1a done, commits
  pending) and confirmed the working tree matched.
- I executed Phase 9 step 9.1b — live incident feed:
  - dashboard/src/feed/rows.ts: unified FeedRow collapsing REST
    IncidentSummary + WS EventRecord; severity->style map
    (CRITICAL red / HIGH orange / MEDIUM amber / LOW sky); clock
    formatter.
  - dashboard/src/feed/FeedPanel.tsx: initial load
    GET /api/incidents?vehicle&limit=50 (server order occurred_at
    DESC), live prepend on Socket.IO "event" with id-dedupe and a
    PER-EVENT vehicleId filter (see lessons — rooms are never left
    server-side), history-unavailable degrade while live keeps
    flowing, 200-row cap.
  - dashboard/src/App.tsx: fixed 9.1a's double socket — ONE ws client
    now feeds both the connection badge and the feed; FeedPanel
    mounted in region-feed; vehicle selection calls subscribeVehicle.
  - Tests: FeedPanel.test.tsx (6) + FeedPanel.reconnect.test.tsx (1,
    real in-process socket.io server: mid-stream engine drop holds
    rendered rows, live resumes from the new sid). Suite now 12/12,
    tsc strict clean.
- I ran the live gate against the real stack (compose kafka/pg/redis +
  host backend + Vite dev + an 8.5-harness-style scratch publisher):
  - Latency publish->DOM: 583 / 244 / 20 / 15 / 43 ms (5/5 < 1 s;
    steady state ~20-45 ms), plus 274 ms on a post-reconnect probe.
  - Reconnect: backend SIGTERM mid-stream -> badge RECONNECTING with
    6/6 rendered rows held (captured in-DOM twice); 2 events published
    during the outage replayed from Kafka on restart -> 8/8 rows; live
    flow resumed on the reconnected socket.
- I cleaned everything: scratch publisher deleted, it91b-% rows purged
  from PG/Redis, dev processes stopped, compose down, Docker Desktop
  quit (machine was docker-down when the session started).
- I answered the owner's scope question honestly: the no-hardware
  chain (video -> edge -> MQTT -> bridge -> Kafka -> backend ->
  dashboard) is built piece-by-piece but has NEVER been run as one
  unbroken arc; "get the output" (evidence clips in the dashboard,
  9.4) and the operator ack loop (9.3) are still missing; I proposed
  the trimmed finish and asked the owner to choose.

CURRENT STATE
-------------
- Uncommitted on main (last commit 1e90445 = phase 8.5):
  - 8.4b (older): M Doc/Implementation/README.md,
    implementation-sequence.md, README.md, infra/docker-compose.yml;
    A .dockerignore, Doc/Implementation/phase-8-completion.md,
    backend/Dockerfile, backend/README.md
  - 9.1a: M .github/workflows/ci.yml; A dashboard/ scaffold (package,
    configs, shell, api/ws clients, ws proof test, README)
  - 9.1b (this session): M dashboard/src/App.tsx; A
    dashboard/src/feed/rows.ts, FeedPanel.tsx, FeedPanel.test.tsx,
    FeedPanel.reconnect.test.tsx
  - A Doc/handoff-2026-08-31.md (rides along with 9.1a's commit or its
    own); this file joins it.
- dashboard: pnpm typecheck clean, pnpm test 12/12. Verified live
  (real stack) on 2026-08-31/09-01.
- Environment clean: no dev servers, no containers, Docker daemon down
  (as found). No scratch files left.

PENDING TASKS
-------------
- OWNER DECISION FIRST (nothing else should start before this). Three
  options I laid out:
  1. Minimal finish (my recommendation): E2E video arc (bundled sample
     video through edge->dashboard, one session) -> 9.3-lite
     (ack/escalate + audit row) -> 9.4-lite (evidence clip plays from
     the incident row — this IS "get the output") -> 9.5 trimmed
     (basic history table) -> DEFER 9.2 camera grid (one sample camera
     adds nothing) -> demo replay + clone test + tag v0.1.0.
  2. Just verify first: run the E2E video arc, owner watches it live,
     then decides.
  3. Stop now: update plan docs to the honest state + park the rest.
- OWNER: commit in order — 8.4b, then 9.1a, then 9.1b. Suggested 9.1b
  message: "feat(dashboard): phase 9 step 9.1b — live incident feed;
  REST history + WS prepend (id-dedupe, per-event vehicle filter vs
  never-left rooms), severity rows, single-socket shell; emit→DOM <1s
  measured (steady ~20-45ms), mid-stream SIGTERM loses zero rows,
  backlog replays; 12 tests"
- If Option 1: NOTE the owner's word "upload" — there is NO upload UI
  anywhere today; the reality is running the edge pipeline (CLI) over
  a bundled/local sample video (edge/configs/cameras.yaml + sample
  videos). If the owner expects a literal upload button in the
  dashboard, that is NEW scope beyond the locked plan — clarify before
  building anything.
- If any option involving the E2E arc: the 8.5 integration harness
  (backend/test/integration/helpers.ts: Publisher, startBackend,
  process-group kill) doubles as the demo-event generator; the Gate-8
  pattern is the template for the video-arc verification.

KEY FILES
---------
- Doc/Implementation/phase-9-plan.md — APPROVED plan; 9.1a-9.7 rows,
  Gate 9 checklist; needs an owner-approved amendment if the trim is
  chosen (do NOT edit before the decision)
- dashboard/src/feed/FeedPanel.tsx (+ rows.ts, 2 test files) — this
  session's deliverable
- dashboard/src/App.tsx — single-socket shell wiring (feed + badge)
- dashboard/src/ws/client.ts — the load-bearing WS client
  (ack'd re-subscribe barrier); src/ws/client.test.ts proves it
- backend/test/integration/helpers.ts — Publisher/startBackend/
  ReceiptCollector; the reusable live-verification + demo generator
- backend/src/ws/push.ts — the WS contract (subscribe joins BOTH
  rooms, never leaves them)
- Doc/handoff-2026-08-31.md — previous arc (9.1a) with its own lessons
- dashboard/README.md — run/test instructions + contract summary

IMPORTANT DECISIONS
-------------------
- Per-event vehicleId filtering in the feed (not room trust): the
  backend's subscribe joins new rooms but NEVER leaves old ones, so a
  vehicle switch would leak the old vehicle's events into the feed.
  This is a backend contract quirk the dashboard now guards against;
  if the backend ever adds room-leaving, the filter stays as defense.
- One WS client per app mount feeds status + feed (9.1a had two
  sockets by accident; fixed during 9.1b wiring).
- Feed rows reset on vehicle switch before the fetch — the REST merge
  must never keep the previous vehicle's rows (bug caught by the new
  tests, regression-locked).
- Scope proposal (pending owner): trim Phase 9 to the no-hardware
  reality — E2E video arc + 9.3-lite + 9.4-lite + basic 9.5 + 9.7
  release; defer 9.2. Rationale: 9.4 is the literal "get the output"
  the owner described; 9.2 needs a real fleet to matter.

EXPLICIT CONSTRAINTS
--------------------
- "Owner runs 100% of git operations" (AGENTS.md permanent rule) —
  agent never commits/stages/pushes; report files + suggested commit
  message, then STOP.
- "Plan -> owner approves -> execute steps ONE BY ONE (per-phase
  working agreement, 2026-08-25)." — the proposed trim is a plan
  change and needs explicit owner approval before execution.
- No auth in the dashboard (owner decision 2026-08-30).
- Runs-without-hardware; CI green without Docker.

CONTEXT FOR CONTINUATION
------------------------
- Live-caught lessons this session: (1) lsof -ti tcp:PORT without
  -sTCP:LISTEN matches proxy CONNECTIONS too — my backend kill took
  Vite down with it (exit 143); kill servers by listener PID only.
  (2) A Vite dev-server restart force-reloads the page (bisects any
  in-browser reconnect proof — rely on captured pre-reload DOM state
  + unit-level reconnect test). (3) RTL findAllByTestId resolves on
  first match, not count — count assertions need waitFor. (4) The
  rooms-never-left quirk above.
- Visual-QA oracle infra still broken (model cascade: unresolved model
  IDs, gemini quota dead) — use direct browser probes, not oracles.
- Dev wiring: dashboard 5173 (Vite proxy /api + /socket.io -> 3000),
  backend 3000 host OR compose, never both (Kafka group steals the
  partition). Compose dashboard host port 8080 reserved for 9.7.
- Node on host v26; CI pins 22. pnpm 11.23.0 everywhere.
- Scratch/live-test hygiene convention: prefix test data (it91b-*),
  purge PG audit-first then events, Redis camera:* + dedupe:* keys,
  delete scratch scripts, leave Docker as found.
- 9.1a lessons still apply (socket.io-client 4.8 non-generic io(),
  disconnect(true) vs conn.close(), vacuous count assertions — assert
  a NEW socket id re-joins).
