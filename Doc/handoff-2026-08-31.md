HANDOFF CONTEXT — Session Log 2026-08-31 (post-crash recovery check; Phase 9 started — 9.1a scaffold + shell EXECUTED)
====================================================================================================================================

Project: MobiSentra AI
Session: after Doc/handoff-2026-08-29-session2.md arc. OpenCode
crashed after the previous session's final report; this session
verified nothing was lost, then executed Phase 9 step 9.1a. Owner has
NOT yet committed 8.4b or 9.1a (git ban: owner does 100% of git).

USER REQUESTS (AS-IS)
---------------------
- "we crashed unexpectedly dont edit or write anything"
- "ok satrt the 9.1a"
- "save the chat"

WORK COMPLETED THIS SESSION
---------------------------

1. CRASH RECOVERY CHECK (read-only, nothing lost)
   - Previous session had finished cleanly (8.4b report delivered
     06:27; Phase 8 CLOSED, Gate 8 3/3). Working tree matched its
     report exactly; no orphan containers/processes; nothing
     half-written.

2. PHASE 9 STEP 9.1a — DASHBOARD SCAFFOLD + SHELL (executed, verified)
   - Delegation to visual-engineering stalled twice (2 x 30 min,
     zero output) -> implemented directly. Lesson: that delegate
     path is unreliable in this environment.
   - dashboard/ is now a standalone pnpm package (repo pattern:
     backend/, bridge/): Vite 7.3.6 + React 19.2.8 + TS 5.9.3
     strict (mirrors backend posture: noUncheckedIndexedAccess,
     exactOptionalPropertyTypes, verbatimModuleSyntax, ...) +
     Tailwind 4.3.3 + socket.io(-client) 4.8.3 + Vitest 3.2.7 +
     @testing-library/react. pnpm-workspace.yaml = allowBuilds
     esbuild (same as backend).
   - src/domain.ts: wire types mirrored 1:1 from backend
     (EventRecord, CameraState, IncidentSummary/Detail, CameraView,
     EventHistoryPage). Backend is the contract; on drift fix
     dashboard types.
   - src/api/client.ts: typed REST, /api base, 6 endpoints, filter
     query-builder. src/ws/client.ts: ack-awaited subscribe +
     unconditional re-subscribe barrier on every reconnect (the
     plan's dead-feed-trap killer), connection-state machine
     (connecting/connected/reconnecting/offline).
   - src/App.tsx: dark-ops shell — header (wordmark, connection
     badge from socket state, vehicle selector from GET /api/cameras
     with graceful degrade: disabled + "registry offline"), 4
     placeholder regions (feed 9.1b / cameras 9.2 / incidents 9.3 /
     history 9.5). Grid: lg:grid-cols-[2fr_3fr],
     lg:grid-rows-[minmax(0,3fr)_minmax(0,3fr)_minmax(0,2fr)], feed
     row-span-2, history col-span-2 — utilities directly on panels,
     no wrapper divs.
   - Vite dev proxy: /api and /socket.io (ws:true) -> :3000. No
     CORS anywhere in dev.
   - Tests 5/5 + tsc clean, no stack needed. WS proof runs a real
     in-process socket.io server (socket.io devDependency):
     engine-level conn.close() drop -> client reconnects -> rooms
     re-joined from NEW sid with fresh ack.
   - Root CI: .github/workflows/ci.yml gained `dashboard` job
     (mirrors backend job: pnpm 11.23.0, node 22, install
     --no-frozen-lockfile, typecheck, test).
   - Verified live in Chromium: a11y snapshot + computed-geometry
     probe; caught and fixed a real layout bug (implicit 3rd grid
     row collapsed panels). Programmatic WCAG contrast audit:
     wordmark 18:1, status 12.6:1, headings 7.1:1, warning 6.9:1;
     fixed zinc-600->zinc-500 on hints/steps (was 2.4:1).
   - dashboard/README.md written (run/test/contract summary).

LIVE-CAUGHT LESSONS (9.1a)
--------------------------
- socket.io-client 4.8's io() entrypoint is NON-generic (TS2558);
  wire types carried by annotated handlers instead.
- Ack-supersession tokens caused a real race (connect-handler
  re-subscribe invalidated the initial pending ack): removed — each
  emit resolves from its own ack/timeout; reconnect barrier
  re-emits for the CURRENT vehicle.
- Test trap: server socket.disconnect(true) = "io server
  disconnect" (client NEVER auto-reconnects by design); a network
  drop is conn.close() ("transport close" -> reconnects). Probed
  empirically with a scratch script.
- Count-based reconnect assertions pass vacuously (initial
  double-emit) — assert a NEW socket id re-joins.
- Visual-QA oracle infra is BROKEN in this environment: model
  cascade fails (opencode/gpt-5-nano + github-copilot/gpt-5.5 not
  found, gemini quota dead, final fallbacks 30-min timeout). Both
  passes produced no verdicts; compensated with geometry probe +
  WCAG audit. Do not wait on oracles until model routing is fixed.

CURRENT STATE
-------------
- Uncommitted on main (last commit 1e90445 = phase 8.5):
  - 8.4b (previous session): M Doc/Implementation/README.md,
    implementation-sequence.md, README.md, infra/docker-compose.yml;
    A .dockerignore, Doc/Implementation/phase-8-completion.md,
    backend/Dockerfile, backend/README.md
  - 9.1a (this session): M .github/workflows/ci.yml; A dashboard/
    (package.json, pnpm-lock.yaml, pnpm-workspace.yaml, tsconfig,
    vite.config.ts, vitest.setup.ts, index.html, .gitignore,
    README.md, src/*)
- dashboard: pnpm typecheck clean, pnpm test 5/5; dev server smoke
  passed (curl 200, /api proxy 500 = backend down, expected).
- Environment clean: dev server stopped, screenshots +
  .playwright-mcp/ artifacts deleted, no containers.

PENDING TASKS
-------------
- OWNER: commit 8.4b first, then 9.1a. Suggested 9.1a message:
  feat(dashboard): phase 9 step 9.1a — scaffold + shell
  (Vite+React19+TS-strict+Tailwind v4); typed api/ws clients, ack'd
  re-subscribe-after-reconnect barrier proven vs in-process server;
  5 tests + CI dashboard job
- NEXT STEP: 9.1b — live incident feed. Feed panel: initial load
  GET /api/incidents, live prepend via "event" on
  alerts:{vehicle_id}, severity-colored rows, status reflects
  socket state. Done-when: with the 8.5 harness publishing, a feed
  row renders <1 s after backend emit (measured emit->DOM);
  reconnect mid-stream loses no rendered rows. Files:
  dashboard/src/feed/* + tests.
- Then 9.2 (camera grid, badge displays edge-computed level —
  never re-derive), 9.3 (incidents+ack/escalate+audit row), 9.4
  (detail+evidence), 9.5 (history+filters), 9.7 (demo+clone test,
  OWNER).

KEY FILES
---------
- Doc/Implementation/phase-9-plan.md — APPROVED plan; 9.1a-9.7 rows,
  defaults table, Gate 9 checklist
- dashboard/src/ws/client.ts — the load-bearing WS client (reconnect
  re-subscribe barrier); its test src/ws/client.test.ts
- dashboard/src/api/client.ts + src/domain.ts — typed REST + wire
  types (backend/src is the source of truth)
- dashboard/src/App.tsx — shell; vehicle selector + connection badge
- dashboard/vite.config.ts — dev proxy (/api, /socket.io ws) + jsdom
  test env
- .github/workflows/ci.yml — dashboard job
- backend/src/ws/push.ts — backend WS contract (subscribe ack joins
  alerts:{v} + cameras:{v}; channels "event"/"state")
- backend/src/lib/events.ts, backend/src/api/store.ts — DTO shapes
- backend/README.md — API surface + env vars (PORT 3000,
  EVIDENCE_ROOT)

IMPORTANT DECISIONS
-------------------
- Direct implementation over delegation when the delegate stalls
  with zero output twice; report it, don't retry a third time.
- One WS client for the whole shell (status socket == subscribing
  socket); created lazily per mount, close() is non-terminal
  (connect() can re-open; close clears the vehicle).
- Placeholder hints/steps at zinc-500 (contrast ~3.9:1, deliberately
  subordinate to zinc-400 headings at 7.1:1) — placeholder copy
  only, real content lands 9.1b+.
- Occupancy badge will DISPLAY the edge-computed level enum
  (owner-approved resolution #2); thresholds never re-derived
  client-side.
- Analytics = Redis live-state only (A1); evidence route exists
  (A2), consumed at 9.4.

EXPLICIT CONSTRAINTS
--------------------
- "Owner runs 100% of git operations" (AGENTS.md permanent rule) —
  agent never commits/stages/pushes; report files + suggested
  message, then STOP.
- Plan -> owner approves -> execute steps ONE BY ONE (per-phase
  working agreement, 2026-08-25).
- No auth in the dashboard (owner decision 2026-08-30).
- Runs-without-hardware; CI green without Docker.

CONTEXT FOR CONTINUATION
------------------------
- Working agreement for next session: owner commits pending work,
  then "start the 9.1b" begins the live feed.
- Dev wiring: dashboard on 5173, backend on 3000 (run backend
  EITHER host OR compose, never both — Kafka group steals the
  partition); compose dashboard host port 8080 reserved for 9.7.
- The 8.5 integration harness doubles as the demo-event generator
  for 9.1b's <1 s emit->DOM measurement.
- Node on this machine is v26; CI pins node 22 — keep code
  compatible with both. pnpm 11.23.0 everywhere.
- If asked to re-run visual QA oracles: infra currently broken
  (see LIVE-CAUGHT LESSONS); use direct browser probes instead.
