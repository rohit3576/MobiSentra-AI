HANDOFF CONTEXT — Session Log 2026-08-24
=========================================

Project: MobiSentra AI (renamed from Civisense AI this session)
Session ID: ses_fccdfe36dffeuJl2ZnOmmNNVzB

USER REQUESTS (AS-IS)
---------------------
- "so just go through the plan doc dont edit or write anything just go through the plan"
- "search on the web what can be the best way to implent this, dont edit or write anything right away"
- "ok make a implemention plan step by step create a doc for that"
- "yea but i want to make it as the personal opensource project so i guess there my be little update in the plan"
- (MobiSentra naming message, ending with) "so need to chnage the name"
- "save the chat"

GOAL
----
Continue MobiSentra AI from the completed planning stage into Phase 0 execution (repo scaffold) whenever the user gives the go-ahead.

WORK COMPLETED
--------------
- I read Doc/plan.md (1261 lines, the original vision doc) and summarized it without editing: modular public-transport vision intelligence layer on existing CCTV, 4 MVP capabilities (person detect+track, crowd/occupancy, fall, fight), rules-not-ML for zones/doors, edge-first architecture, event+severity engine, Kafka→Node.js→PG/Redis→React dashboard, 8-phase roadmap.
- I ran 4 parallel librarian web-research agents (detection+tracking, pose/fall/fight, edge deployment, backend/streaming/MLOps) and synthesized a validated stack with 5 key deltas vs the plan doc:
  1. Don't put Kafka on the vehicle — MQTT QoS 1 + SQLite disk spool at edge → EMQX bridge → Kafka server-side (intermittent vehicle connectivity loses data with Kafka-on-bus).
  2. Skip RT-DETR — YOLO11n beats it on edge NPUs; proven 32 FPS @ 7W on Jetson Orin Nano.
  3. Fight detection: start with pretrained MoViNet4Violence (open license, streaming, 5.1MB INT8) instead of training from scratch; RWF-2000 dataset is non-commercial-restricted — use Hockey Fights / UBI-Fight.
  4. DeepStream Python bindings deprecated (DS 9.0+) — custom Python pipeline for MVP; DeepStream Service Maker only at fleet scale (10+ vehicles).
  5. kafkajs unmaintained — use @confluentinc/kafka-javascript v1.10+.
  - Field lesson: "Safety After Dark" Wollongong transit deployment = 97% validation accuracy but 23% field false-positive rate → evidence aggregation + negatives collection are mandatory.
- I created Doc/implementation-plan.md: 11 phases (0-10), each with goal/steps/gate; locked decision table; repo structure; ~14 weeks to v0.1.0 MVP demo.
- I revised it into the personal-open-source edition: new §0 Open-Source Ground Rules (AGPL-3.0 forced by ultralytics dependency; dependency license table; runs-without-hardware principle with bundled sample videos as default input; fully OSI-open compose stack — Apache Kafka KRaft instead of Redpanda/BSL), CI from commit #1, Phase 10 Jetson marked stretch, v0.1.0 release milestone at Phase 9 gate, §9 Community & Release Cadence, §10 Responsible Use Policy, license-contamination + solo-burnout risks added.
- I renamed the project Civisense → MobiSentra across both docs (all occurrences, Kafka topics → mobisentra.*, repo root → mobisentra/, ASCII diagrams re-aligned). New tagline: "AI Vision Intelligence for Safer Public Mobility". Verified zero "civisense" references remain (case-insensitive grep).
- I saved this session log / handoff doc.

CURRENT STATE
-------------
- Repo workspace: /Users/rohitashokpawar/Personal/CivisenseAI (folder itself NOT yet renamed — user's call, mv command suggested)
- NOT a git repo yet — no git init/commits have ever been run (owner does all git ops per permanent rule)
- Doc/plan.md — original vision doc, fully renamed to MobiSentra AI, tagline updated
- Doc/implementation-plan.md — open-source-edition step-by-step plan, fully renamed
- Doc/handoff-2026-08-24.md — this file
- No code written yet; no scaffold exists yet; Phase 0 not started

PENDING TASKS
-------------
- User decision: rename workspace folder CivisenseAI → MobiSentraAI (I suggested: mv ~/Personal/CivisenseAI ~/Personal/MobiSentraAI)
- Phase 0 execution (awaiting user go-ahead): git init by owner, public GitHub repo, LICENSE (AGPL-3.0), README, CONTRIBUTING, CoC, issue templates, monorepo scaffold (uv + pnpm + strict TS), infra/docker-compose.yml (Kafka KRaft + EMQX + PostgreSQL + Redis + MLflow), GitHub Actions CI, MQTT→Kafka round-trip smoke test, event schema v0 (CloudEvents, topics mobisentra.detection/events/alerts/analytics), camera registry YAML + sample-cameras.yaml
- Then Phases 1-10 per Doc/implementation-plan.md, each gated
- Pre-commercial (later): trademark checks (IP India, USPTO, EUIPO) + domain availability for MobiSentra — empty web search does not guarantee availability
- No todos were tracked via todowrite this session (planning-only session)

KEY FILES
---------
- Doc/plan.md - original vision/architecture doc (MobiSentra AI, renamed)
- Doc/implementation-plan.md - the authoritative step-by-step execution plan (open-source edition) — start here
- Doc/handoff-2026-08-24.md - this session log/handoff

IMPORTANT DECISIONS
-------------------
- Name: MobiSentra AI ("AI Vision Intelligence for Safer Public Mobility"); GitHub description and README tagline already drafted (in implementation-plan.md §0)
- License: AGPL-3.0 (ultralytics is AGPL; simplest clean path; documented in §0 with dependency license table)
- Vision stack: YOLO11n + ByteTrack (static cams) / BoT-SORT (moving bus cams) + YOLO11-pose; fall = keypoint rules + temporal classifier hybrid (UR Fall/Le2i benchmarks); fight = MoViNet4Violence pretrained behind event engine, VideoMAE fine-tune post-MVP
- Messaging: MQTT QoS 1 + SQLite spool at edge → EMQX rule-engine bridge → Apache Kafka (KRaft) server-side; CloudEvents envelope; Redis SETNX dedupe on consumer
- Backend: Node.js + TypeScript, @confluentinc/kafka-javascript, PG history + Redis live state + Socket.IO rooms per vehicle; React + TS + Tailwind dashboard; auth off by default (env-flag)
- OSS principles: runs-without-hardware (bundled sample videos default), sample-data-first (open datasets only), fully OSI-open compose stack, CI from commit #1, Jetson = stretch not core
- Verification doctrine: hard gate per phase; clone test after Phases 0/2/9; fault-injection tests for messaging/backend
- Every phase gate defined with measurable criteria (e.g., fall: ≥90% on UR Fall, <2 FP/hr; messaging: 10-min blackout → zero loss, zero dupes)

EXPLICIT CONSTRAINTS
--------------------
From owner's permanent AGENTS.md (git ban):
- "I must NEVER, under ANY circumstance: Run `git commit` … `git push` … ANY git write command: `git add`, `git stash`, `git reset`, `git rebase`, `git cherry-pick`, `git revert`, `git merge`, `git restore`, `git rm`, `git mv`, `git tag`, `git branch`, `git checkout -b`, `git clean` … any GitHub-mutating command"
- "The OWNER does 100% of commits, pushes, and git history operations. Always."
- Handoff pattern: report exact changed file list + suggested commit message; owner reviews and commits.

CONTEXT FOR CONTINUATION
-------------------------
- This session was planning-only: research → plan doc → OSS adjustments → rename. No code exists yet.
- Doc/implementation-plan.md is the single source of truth for execution; Doc/plan.md is the background vision.
- The four research reports (full source lists: Ultralytics docs, NVIDIA DeepStream, OmniFall/RWF-2000 papers, Confluent/CloudEvents specs, Frigate/MetroEye/Safety-After-Dark references) live only in this chat history, not on disk — the distilled conclusions are baked into the implementation plan's decision table and risk register.
- Suggested first commit message (for owner to run): "docs: add research-backed implementation plan (open-source personal project edition)"
- Gotcha: workspace folder is still CivisenseAI; if renamed, update any editor/terminal paths before continuing.
- Gotcha: RWF-2000 dataset must never be bundled or used for anything commercial; Hockey Fights / UBI-Fight are the open alternatives.
- Next session should ask: "Start Phase 0?" and follow Doc/implementation-plan.md §4 Phase 0 step by step.
