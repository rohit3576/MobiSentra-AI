export const BACKEND_VERSION = "0.1.0";

console.log(
  `mobisentra backend scaffold v${BACKEND_VERSION} — ` +
    "consumer/API/WebSocket services arrive in Phase 8 " +
    "(see Doc/Implementation/implementation-sequence.md)"
);
